# ---------- Line endings ----------
# Default to LF for all text files (portable across macOS/Linux/Windows)
* text=auto eol=lf
# Keep Windows scripts with CRLF so they run correctly
*.bat text eol=crlf
*.cmd text eol=crlf
# Shell/Unix scripts: always LF
*.sh  text eol=lf
# PowerShell generally expects CRLF
*.ps1 text eol=crlf

# Optional: enforce UTF-8 for working tree (modern projects)
* working-tree-encoding=UTF-8

# ---------- Binary vs text ----------
# Images, fonts, archives, media are binary (no textual diffs)
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.avif binary
*.ico  binary
*.pdf  binary
*.zip  binary
*.gz   binary
*.bz2  binary
*.7z   binary
*.tar  binary
*.mp4  binary
*.mp3  binary
*.wav  binary
*.mov  binary
*.woff  binary
*.woff2 binary
*.ttf   binary
*.eot   binary

# SVG is text—keep it diffable
*.svg text

# Source maps are huge/noisy; show as changed but don’t print diffs
*.map -diff

# TypeScript declarations: readable, keep diffs; maps are noisy
*.d.ts     text
*.d.ts.map -diff

# ---------- Lockfiles & minified bundles ----------
# Show lockfiles as single “blob” diffs to reduce PR noise
yarn.lock        -diff
pnpm-lock.yaml   -diff
package-lock.json -diff
bun.lockb        -diff

# Minified output is machine-generated—don’t diff it
*.min.js  -diff linguist-generated
*.min.css -diff linguist-generated

# ---------- Generated/build outputs (if ever present in repo) ----------
.next/**  -diff linguist-generated
out/**    -diff linguist-generated
build/**  -diff linguist-generated
coverage/** -diff linguist-generated

# ---------- GitHub Linguist tuning ----------
# If you vendored third-party code anywhere:
vendor/**            linguist-vendored
public/**/vendor/**  linguist-vendored
# Mark minified assets in public as generated
public/**/*.min.*    linguist-generated
# Docs count as docs, not app code
docs/**              linguist-documentation

# ---------- Archive exports (git archive) ----------
# Exclude dev-only folders/files from release tarballs
.github/**    export-ignore
.vscode/**    export-ignore
.idea/**      export-ignore
*.local.*     export-ignore
.env*         export-ignore
scripts/**    export-ignore
docker/**     export-ignore
**/*.test.*   export-ignore
**/__tests__/** export-ignore
**/__mocks__/**  export-ignore
